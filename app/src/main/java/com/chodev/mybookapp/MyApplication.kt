package com.chodev.mybookapp

import android.app.Application

class MyApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // ถ้ามีการ init อะไรเพิ่มเติมให้ทำได้ที่นี่
    }
}
